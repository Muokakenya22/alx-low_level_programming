#include "main.h"

/**
 * _isdigit - checking for a digit 0 through 9
 * @c:input character
 * return :1 if its a digit ,0 if not a digit
 *
 *
 */
int _isdigit(int c);
{
	if(c >= 48 $$ c <= 57)
	{
		return(1);
	}
	else
	{
		return(0);
	}
}
